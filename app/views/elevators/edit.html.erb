<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing elevator</h1>

  <%= render "form", elevator: @elevator %>

  <%= link_to "Show this elevator", contract_elevator_path(@contract, @elevator), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  <%= link_to "Back to elevators", contract_elevators_path(@contract), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
