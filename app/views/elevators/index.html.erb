<div class="mx-auto w-1/3 mt-4">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="my-4">
    <h1 class="font-bold text-4xl"> <%= @contract.job_name %> Elevators</h1>
    <%= render @elevators %>
  </div>
  
</div>
