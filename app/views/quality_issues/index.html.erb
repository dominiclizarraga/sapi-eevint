<%= render partial: "/contracts/sidebar-2" %>


<div class="mx-auto ml-8 mt-4 w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>
  
  <h1 class="font-bold text-4xl mb-4"> Quality Issues</h1>
  <%= link_to 'New Quality Issue', new_quality_issue_path, class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mb-4' %>


  <div class="flex flex-wrap">
    <%= render @quality_issues %>
  </div>
</div>
